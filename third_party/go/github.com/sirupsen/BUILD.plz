package(default_visibility = ["PUBLIC"])

go_module(
    name = "logrus",
    module = "github.com/sirupsen/logrus",
    version = "v1.8.1",
    deps = [
        "//third_party/go/golang.org/x:sys",
    ],
)
